import { StyleFooter } from "../Style/StyleFooter"

export default function Footer ( {textFooter} ){

    return(
        <StyleFooter>
            <div>
                <p>{textFooter}</p>
            </div>
        </StyleFooter>
    )

}